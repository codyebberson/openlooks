import { DocPage } from '../components/DocPage/DocPage';
import { Box } from '../lib/index';

<DocPage title="Box" subtitle="Add system styles to any element or component">

## Usage

<Box p="xl" radius="xl" style={{ 'background-color': 'var(--oc-gray-1)' }}>
  Box lets you add system styles to any element or component.
</Box>

## Polymorphic component

Box is a polymorphic component, which means you can change the root element:

<Box component="a" href="#" style={{ 'background-color': 'var(--oc-gray-1)' }}>Set component prop to style different elements</Box>

</DocPage>
